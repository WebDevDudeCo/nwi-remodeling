// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Temporarily disable CSS modules for debugging
  // css: {
  //   modules: {
  //     generateScopedName: '[name]__[local]___[hash:base64:5]'
  //   }
  // },
});
